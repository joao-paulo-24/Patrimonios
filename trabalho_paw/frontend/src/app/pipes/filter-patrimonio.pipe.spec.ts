import { FilterPatrimonioPipe } from './filter-patrimonio.pipe';

describe('FilterPatrimonioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPatrimonioPipe();
    expect(pipe).toBeTruthy();
  });
});
